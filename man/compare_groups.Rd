% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_data.R
\name{compare_groups}
\alias{compare_groups}
\title{Compare microCT data between two treatments}
\usage{
compare_groups(data, type = NULL, measures = NULL, test = "parametric")
}
\arguments{
\item{data}{Bone microCT data in a data frame, formatted as is the output
of \code{\link[=read_trabecular]{read_trabecular()}} or \code{\link[=read_cortical]{read_cortical()}}. Note that data for only one
Site should be supplied at a time.}

\item{type}{A string indicating the type of data supplied. Options include
\code{"trabecular"} or \code{"cortical"}. Defaults to \code{NULL}, in which case the
function will try to figure out which type of data it is.}

\item{measures}{Which bone parameters to analyze. There are three options:
\itemize{
\item \code{NULL} (the default): analyze a sensible subset of commonly reported
parameters. For trabecular data these are stored in
\link{default_trabecular_measures}; for cortical data, in
\link{default_cortical_measures}.
\item \code{"all"}: analyze every detected parameter of the appropriate type.
\item A character vector of specific column names (e.g.,
\code{c("Tb.BV/TV", "Tb.Th")}).
}}

\item{test}{A string indicating which statistical test to use. Options are
\code{"parametric"} (default) for a Student's \emph{t}-test, or \code{"nonparametric"}
for a Wilcoxon rank-sum test.}
}
\value{
A list of each bone measure analyzed. Each measure is itself a list
of each sex analyzed. Each sex is a tibble/data frame containing columns
for \code{Sex}, \code{Treatment}, \code{n}, \code{Mean}, \code{SEM}, \code{P}, and \code{Sig}
(a string containing \code{"*"} if \emph{P} < 0.05, \code{"**"} if \emph{P} < 0.01, or
\code{"***"} if \emph{P} < 0.001). When \code{test = "nonparametric"}, \code{Mean} is replaced
by \code{Median} and \code{SEM} by \code{IQR}.
}
\description{
This function takes in trabecular or cortical bone microCT data at one site
and compares it between two treatments. By default, each sex is compared
separately. A parametric (Student's \emph{t}-test) or nonparametric
(Wilcoxon rank-sum test) approach can be selected.
}
\examples{
mouse_table <- read_mouse_table(mctr_ex("CD2MouseTable.csv"))
spine_trab <- read_trabecular(mctr_ex("spine_trabecular_data.csv"),
                              mouse_table, site = "Spine")

# Default measures, parametric
spine_res <- compare_groups(spine_trab)

# All available trabecular measures
spine_all <- compare_groups(spine_trab, measures = "all")

# Specific measures, nonparametric
spine_custom <- compare_groups(spine_trab,
                               measures = c("Tb.BV/TV", "Tb.Th"),
                               test = "nonparametric")
}
