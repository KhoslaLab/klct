% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_data.R
\name{plot_groups}
\alias{plot_groups}
\title{Plot bone microCT data by treatment}
\usage{
plot_groups(
  data,
  type = NULL,
  measures = NULL,
  test = "parametric",
  title = "sex",
  x_axis_angle = 45
)
}
\arguments{
\item{data}{A tibble/data frame containing bone microCT data, formatted as is
the output of \code{\link[=read_trabecular]{read_trabecular()}} or \code{\link[=read_cortical]{read_cortical()}}.}

\item{type}{A string indicating the type of data supplied. Options include
\code{"trabecular"} or \code{"cortical"}. Defaults to \code{NULL}, in which case the
function will try to figure out which type of data it is.}

\item{measures}{Which bone parameters to plot. There are three options:
\itemize{
\item \code{NULL} (the default): plot the sensible subset of commonly reported
parameters defined in \link{default_trabecular_measures} or
\link{default_cortical_measures}.
\item \code{"all"}: plot every detected parameter of the appropriate type.
\item A character vector of specific column names (e.g.,
\code{c("Ct.Ar", "Ct.Th")}).
}}

\item{test}{A string indicating which statistical test to use for
significance annotations. Options are \code{"parametric"} (default) for a
Student's \emph{t}-test, or \code{"nonparametric"} for a Wilcoxon rank-sum test.}

\item{title}{A string indicating what type of title the plots should have.
Defaults to \code{"sex"}, which is currently the only option implemented. To
remove titles from the plots, set \code{title} to \code{NULL}.}

\item{x_axis_angle}{The angle at which to rotate the x-axis labels so they
don't overlap, passed on to \code{\link[ggplot2:theme]{ggplot2::theme()}} as
\code{axis.text.x = element_text(angle = x_axis_angle)}.}
}
\value{
A list containing \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} objects for each sex analyzed.
To print each plot without printing the list index, see \code{\link[=print_plots]{print_plots()}}.
}
\description{
This function takes in bone microCT data and produces boxplots comparing two
treatments. Each sex is plotted separately.
}
\details{
If the two treatments are significantly different, statistical significance
will be indicated with \code{"*"} if \emph{P} < 0.05, \code{"**"} if \emph{P} < 0.01, or
\code{"***"} if \emph{P} < 0.001.
}
\examples{
mouse_table <- read_mouse_table(klct_ex("mouse-table.csv"))
met_cort <- read_cortical(klct_ex("femur_metaphysis_data.csv"),
                          mouse_table, site = "Met")

# Default measures
plot_groups(met_cort) |> print_plots()

# All cortical measures
plot_groups(met_cort, measures = "all") |> print_plots()

# Specific measures, nonparametric
plot_groups(met_cort, measures = c("Ct.Th", "Ct.vBMD"),
            test = "nonparametric") |> print_plots()
}
