% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create.R
\name{create_two_treatment_comparison}
\alias{create_two_treatment_comparison}
\alias{create_two_treatment_two_sex_comparison}
\title{Create a project skeleton to compare bone microCT data}
\usage{
create_two_treatment_comparison(
  file_name = paste0(format(Sys.Date(), "\%F"), "-microCT-Analysis.Rmd"),
  mayo = FALSE
)

create_two_treatment_two_sex_comparison(
  file_name = paste0(format(Sys.Date(), "\%F"), "-microCT-Analysis.Rmd"),
  mayo = FALSE
)
}
\arguments{
\item{file_name}{A string to name the project analysis R Markdown document.
Defaults to a file in the style of \code{"YYYY-MM-DD-microCT-Analysis.Rmd"},
where \code{YYYY-MM-DD} represents today's date.}

\item{mayo}{Logical. If \code{TRUE}, create a Mayo-themed template using
\code{mayodown::mayohtml}. If \code{FALSE} (the default), use
\code{rmarkdown::html_document}.}
}
\value{
Returns \code{TRUE} if successful.
}
\description{
These functions assume you are in a project root directory and create an R
Markdown file containing a pre-filled bone microCT analysis. They create
the analysis R Markdown file using the appropriate package template.
}
\details{
\code{create_two_treatment_comparison()} creates a template for comparing two
treatment groups, analyzing each sex separately.

\code{create_two_treatment_two_sex_comparison()} creates a template for a full
two-way (Treatment x Sex) analysis.

Both functions accept a \code{mayo} argument. When \code{mayo = TRUE}, the template
uses \code{mayodown::mayohtml} for a Mayo-themed report. When \code{mayo = FALSE}
(the default), the template uses \code{rmarkdown::html_document}.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
create_two_treatment_comparison()
create_two_treatment_comparison(mayo = TRUE)
create_two_treatment_two_sex_comparison()
create_two_treatment_two_sex_comparison(mayo = TRUE)
\dontshow{\}) # examplesIf}
}
