# Non-measure metadata columns excluded during detection

Non-measure metadata columns excluded during detection

## Usage

``` r
metadata_cols
```

## Format

An object of class `character` of length 8.
