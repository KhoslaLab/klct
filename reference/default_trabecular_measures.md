# Default trabecular measures

The subset of trabecular bone parameters reported by default:
`Tb.BV/TV`, `Tb.Th`, `Tb.Sp`, and `Tb.N`.

## Usage

``` r
default_trabecular_measures
```

## Format

An object of class `character` of length 4.
